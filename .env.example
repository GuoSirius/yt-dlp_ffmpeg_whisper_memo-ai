# =============================================================================
# 视频处理流水线 - 环境变量配置模板
# =============================================================================
# 用法：
#   1. 复制此文件:  cp .env.example .env
#   2. 修改 .env 中的值以适配你的 Excel 结构和平台
#   3. 运行脚本:    python process_videos.py  (或 node process_videos.js)
#
# 核心思路：只需提供 Excel + 列名映射 + 平台 URL 模板，
# 脚本自动完成 下载 → 转码 → 语音识别 → AI 关键词归纳 全流程。
# =============================================================================
#
# ── 配置项变更权限说明 ──────────────────────────────────────────────────────
# 为方便修改，每个配置项用以下标记标注变更规则：
#
#   【自由】= 值可随意变更为任意合法内容（路径、开关、数字、字符串等）
#   【调序】= 值来自一个固定集合，可以：增减条目 / 调整顺序 / 改优先级
#            但不能使用集合之外的值（否则脚本无法识别）
#   【关联】= 值需与脚本内约定的 Key 名一致（通常跟随 PLATFORM_PRIORITY）
#   【固定】= 除非 Excel 列名或脚本内部逻辑改变，否则不应修改
#
# =============================================================================


# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  一、输入与字段映射（必设 — 不设置无法运行）                             ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ── 1.1  输入文件 ───────────────────────────────────────────────────────────
# 【自由】任意合法的 Excel 文件路径（相对或绝对）
EXCEL_FILE=data/examples/website_split_demo.xlsx

# ── 1.2  处理的 Sheet ───────────────────────────────────────────────────────
# 【自由】逗号分隔的 sheet 名称列表，可增删、调序；不指定则处理所有 sheet
VIDEO_SHEETS=YouTube视频,普诺赛中文站

# ── 1.3  Excel 字段映射 ─────────────────────────────────────────────────────
# 以下变量值 = 你的 Excel 表头列名。
# 【固定】除非 Excel 列名发生变化，否则不应修改

# ════════════ 必填列 ════════════
# 唯一标识（行匹配 + 文件命名）
COL_ID=extra.id

# 视频标题
COL_TITLE=title

# 输出列：识别文本写入此列
COL_CONTENT=content

# 输出列：AI 分析结果（关键词归纳）写入此列
COL_KEYWORDS=keywords

# ════════════ 平台视频 ID 列 ════════════
# 至少配置一个，列值会替换 URL 模板中的占位符
COL_TENCENTVID=extra.tencentVid
COL_BILIBILIBVID=extra.bilibiliBvid
COL_YOUTUBEID=extra.youtubeId
COL_YOUKUID=extra.youkuId


# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  二、平台与下载（必设 — 决定从哪些平台下载及如何下载）                   ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ── 2.1  平台优先级 ─────────────────────────────────────────────────────────
# 【调序】可选值来自以下固定集合（与上方平台列名后缀对应）:
#   bilibili   = B 站 BV 号
#   youtube    = YouTube 视频 ID
#   tencent    = 腾讯视频 VID
#   youku      = 优酷视频 ID
#
# 你可以：
#   1. 调整顺序 → 排前面的平台优先下载
#   2. 增减条目 → 只保留你需要的平台
#   ✗ 不能使用集合之外的值（如 "tiktokId"、"douyinId" 等未定义的 key）
PLATFORM_PRIORITY=bilibili,youtube,tencent,youku

# ── 2.2  平台 URL 模板 ──────────────────────────────────────────────────────
# 【关联】模板中的 {占位符} 必须与 PLATFORM_PRIORITY 中的 key 名一致
#         如 {youtube} 会被 COL_YOUTUBEID 列的值替换
# 【自由】URL 本身可随意修改（例如从 youtu.be 改为 www.youtube.com/watch?v=）
#
# 前缀规则: TENCENT=腾讯视频  BILIBILI=B站  YOUTUBE=YouTube  YOUKU=优酷
TENCENT_URL_TPL=https://v.qq.com/x/page/{tencent}.html
BILIBILI_URL_TPL=https://www.bilibili.com/video/{bilibili}/
YOUTUBE_URL_TPL=https://youtu.be/{youtube}
YOUKU_URL_TPL=https://v.youku.com/v_show/id_{youku}.html

# ── 2.3  平台通用配置变量参考 ───────────────────────────────────────────────
# 以下变量对每个平台分别配置，前缀 = 平台前缀（BILIBILI / YOUTUBE / TENCENT / YOUKU）
# 详细说明见下方各平台专属配置段（2.5 ~ 2.8）
#
# {前缀}_COOKIE_FILE            【自由】cookie 文件路径（留空=不使用）
# {前缀}_COOKIES_FROM_BROWSER   【自由】chrome / firefox / edge / 留空
#                                 优先于 COOKIE_FILE，直接从浏览器读 cookie
#                                 ⚠️ Windows Chromium 系浏览器 DPAPI 解密已知失败
# {前缀}_PROXY                  【自由】代理地址 (http:// 或 socks5://)
#                                 如 Clash 默认 http://127.0.0.1:7890
#                                 ⚠️ 脚本会自动给 yt-dlp 及其 node/ejs 子进程注入
#                                    HTTPS_PROXY 环境变量
#                                 ⚠️ 换代理客户端后端口常变，端口填错该平台会
#                                    100% 下载失败；各客户端默认端口见 2.4.4 速查表
#                                    本机实际监听端口自查见 2.4.4 的 netstat/lsof 命令
# {前缀}_USER_AGENT             【自由】自定义 User-Agent (留空=yt-dlp 默认)
#                                 含特殊字符需用双引号包裹
# {前缀}_FORMAT                 【自由】yt-dlp 下载格式
#                                 默认 bestvideo+bestaudio/best
# {前缀}_CONCURRENT_FRAGMENTS   【自由】并发分片数 (加速下载, 仅 B站)
# {前缀}_JS_RUNTIMES            【自由】JS 运行时 (node/deno, YouTube n-sig 求解)
# {前缀}_REMOTE_COMPONENTS      【自由】远程组件 (如 ejs:github, YouTube 必需)
# {前缀}_REFERRER               【自由】Referer 请求头 (如 B站)
# {前缀}_URL_TPL                【自由】URL 模板 (见 2.2 "平台 URL 模板")

# ── 2.4  输出目录（相对于项目根目录）────────────────────────────────────────
# 【必填】输出根目录（默认 output）。子目录名固定，自动创建：
#   downloads/   yt-dlp 原始下载
#   transcoded/  ffmpeg 转出的音频
#   transcripts/ whisper 识别文本（断点续跑校验依据）
#   keywords/    AI 关键词
#   reports/     执行报告 JSON
#   progress/    增量进度 JSON（每任务完成即时写入）
#   logs/        运行日志/console-ui 输出
# 【CLI】--output <dir> 优先级最高
OUTPUT_DIR=output

# 【必填】Cookie 文件目录（与 OUTPUT_DIR 独立，不归并到 OUTPUT_DIR 下）
COOKIES_DIR=data/cookies

# ── 2.4.0  模型缓存目录（C 盘紧张时把模型下载重定向到其他盘）────────────────
# 以下变量分别控制 Whisper / faster-whisper / FunASR / PaddleOCR 的模型缓存位置。
# 直接写在这里（.env）即可，脚本会把它们继承给所有子进程（huggingface-hub / paddle 等库自动读取）；
# C 盘空间不足导致模型下载失败时，务必改到其他盘。PADDLE_OCR_BASE_DIR 由本脚本专门读取并透传。
#
# 【自由】whisper 本地模型缓存（openai-whisper 的 WHISPER_MODEL_DIR 也认这个）
# XDG_CACHE_HOME=D:\models
# 【自由】faster-whisper / HuggingFace 模型缓存
# HF_HOME=D:\models\huggingface
# 【自由】FunASR / ModelScope 模型缓存
# MODELSCOPE_CACHE=D:\models\modelscope
# 【自由】PaddleOCR 模型缓存（脚本读取后会透传为 PADDLE_PDX_CACHE_HOME / PADDLEOCR_HOME）
# PADDLE_OCR_BASE_DIR=D:\AI_Models\PaddleOCR

# ── 2.4.1  HuggingFace 镜像（faster-whisper / funasr 模型下载加速）────────
# 留空 = 直连 huggingface.co（国外网络或已缓存模型时可留空）
# 【自由】国内网络访问 huggingface.co 不稳定时，设为清华镜像:
HF_ENDPOINT=https://hf-mirror.com

# ── 2.4.2  产物最小长度（断点续跑校验阈值，字符数）────────────────────
# 【自由】低于此值视为残缺产物，会被删除并重做
MIN_TRANSCRIPT_CHARS=50
MIN_KEYWORDS_CHARS=5

# ── 2.4.3  Excel 实时写回落盘间隔（秒）─────────────────────────────────
# 【自由】单位统一为「秒」，Python / JS 两端写法一致（JS 内部自动 ×1000）
# 任务完成后先写入内存缓存，再由后台线程/定时器按此间隔落盘。
# 强杀（kill -9）最坏丢失一个间隔内的修改；正常 Ctrl+C / 退出有信号兜底，不丢。
# 建议 3~5：值越小越安全、磁盘写越频繁；值越大越省 I/O。不建议 >5。
EXCEL_FLUSH_INTERVAL=3

# ── 2.4.3b  Excel 被占用时的等待策略（秒）─────────────────────────────────
# 若你中途用 Excel 打开查看 .xlsx，文件会被锁定、实时写回暂时失败。
# 此时控制台只告警一次，任务继续跑；关闭 Excel 后下一次落盘自动恢复（脏数据一直在内存，不丢）。
# 若任务跑完文件仍被占用：进程会阻塞等待直到写成功再退出，而非静默丢数据。
# 【自由】0 = 无限等待；建议 300（5 分钟）。超过上限仍被占用 → 把全部改动另存为 <Excel名>.pending.xlsx 兜底。
EXCEL_LOCK_MAX_WAIT=300

# ── 2.4.4  代理预检（TCP 探测超时，毫秒）───────────────────────────────────
# 含 download 步骤时，会对所有配置了 {前缀}_PROXY 的平台做一次 TCP 连通性探测。
# 探测失败 → 预检阶段直接报出端口和对应 env 名，避免整批任务白跑重试。
# 【自由】0 = 关闭预检；建议 2000（同一 proxy 只探一次，几乎不增加启动耗时）
PROXY_PROBE_TIMEOUT=2000
#
# 常用代理客户端「本地监听端口」速查表（换客户端时照此核对，端口填错 = 该平台全失败）
#   客户端                          HTTP 代理    SOCKS5      混合端口(mixed)
#   ───────────────────────────────────────────────────────────────────
#   Clash / Clash Verge / Mihomo       7890       7891        7893
#   Clash for Windows (CFW)            7890       7891        7893
#   v2rayN (Windows)                   10809      10808       (默认开 SOCKS 10808)
#   v2rayNG (Android)                  10809      10808       —
#   Shadowsocks / SSR                   —         1080        —
#   ShadowsocksX-NG (macOS)             —         1080        —
#   其它 / 自定客户端（含你当前的「魔戒」）
#                                 → 端口不固定，以客户端界面「本地代理 / 监听端口」为准；
#                                   用下方 netstat / lsof 命令自查本机实际监听端口
#   ⚠️ 本项目用「混合端口」或「HTTP 端口」最稳；填 SOCKS 时务必写 socks5:// 前缀
#   ⚠️ 7897 不是任何客户端的默认端口，是早期填错的示例，请以你客户端实际为准
#
# 代理端口对不上时如何定位（换代理客户端后最常见的坑）：
#   1. 代理客户端里找「混合端口 / Mixed Port / HTTP 端口」，这个才是要填的端口
#   2. 或列出本机监听端口：
#        Windows:  netstat -ano | findstr LISTENING | findstr 127.0.0.1
#        macOS/Linux: lsof -nP -iTCP -sTCP:LISTEN | grep 127.0.0.1
#   3. 单独验证某端口能否翻墙（换成你的端口）：
#        yt-dlp --proxy http://127.0.0.1:7890 --skip-download --get-title <YouTube链接>
#   4. 改完 .env 后干跑确认：
#        node process_videos.js --step download --dry-run     # 看「代理: ✅」

# ── 2.5  Bilibili 配置 ──────────────────────────────────────────────────────
# 方案 A（推荐）: 直接从 Firefox 浏览器读 cookie，yt-dlp 能稳定解密
# 【调序】firefox / chrome / edge
BILIBILI_COOKIES_FROM_BROWSER=firefox

# 方案 B（备用）: 从文件读取 cookie
# 【自由】cookie 文件路径（方案 A 启用时此行无效）
# BILIBILI_COOKIE_FILE=cookies/bilibili.txt

# 【自由】自定义 User-Agent (含特殊字符需用双引号包裹)
BILIBILI_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36"

# 【自由】
BILIBILI_REFERRER=https://www.bilibili.com/

# 【自由】并发分片数
BILIBILI_CONCURRENT_FRAGMENTS=4

# 【自由】
BILIBILI_FORMAT=bestvideo[height<=720]+bestaudio/bestvideo+bestaudio/best

# ── 2.6  YouTube 配置 ───────────────────────────────────────────────────────
# 方案 A（推荐）: 直接从 Firefox 浏览器读 cookie，yt-dlp 能稳定解密
#   ⚠️ Windows Chromium 系浏览器 DPAPI 解密已知失败，不建议用 --cookies-from-browser chrome
# 【调序】firefox / chrome / edge
YOUTUBE_COOKIES_FROM_BROWSER=firefox

# 方案 B（备用）: 从文件读取 cookie，但需定期更新（YouTube 约 48h 轮换 cookie）
#   Chrome 用 "Get cookies.txt LOCALLY" 扩展导出 → cookies/youtube.txt
# 【自由】cookie 文件路径
# YOUTUBE_COOKIE_FILE=cookies/youtube.txt

# 【自由】代理地址（Clash 默认 7890 / v2rayN 默认 10809 / CFW 默认 7890，完整见 2.4.4 速查表）
# ⚠️ 必须填代理客户端「实际监听」的混合/HTTP 端口，端口填错 → YouTube 全部下载失败
#    换过代理客户端就务必核对一次，定位方法见 2.4.4
#    示例值 7890 仅作占位，请改成你客户端真实端口（你当前「魔戒」本机实听 17890）
YOUTUBE_PROXY=http://127.0.0.1:7890

# 【自由】自定义 User-Agent (含特殊字符需用双引号包裹)
YOUTUBE_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36"

# 【自由】解 n-sig 的 JS 运行时
YOUTUBE_JS_RUNTIMES=node

# 【自由】YouTube n-sig 需要的远程组件
YOUTUBE_REMOTE_COMPONENTS=ejs:github

# 【自由】
YOUTUBE_FORMAT=bestvideo[height<=720]+bestaudio/bestvideo+bestaudio/best

# ── 2.7  腾讯视频配置 ───────────────────────────────────────────────────────
# 【自由】腾讯视频通常不需要 cookie
TENCENT_COOKIE_FILE=

# 【自由】自定义 User-Agent (解决 Windows 下 latin-1 编码错误)
TENCENT_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"

# ── 2.8  优酷配置 ───────────────────────────────────────────────────────────
# 【自由】
YOUKU_COOKIE_FILE=

# 【自由】
YOUKU_USER_AGENT=


# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  三、AI 关键词分析（必设 — 启用时需配置 API）                            ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ── 3.1  开关 ───────────────────────────────────────────────────────────────
# 【调试】AI_DEBUG=true 时打印实际发送的 prompt 和 AI 返回内容（排查关键词质量问题）
AI_DEBUG=false

# 【自由】是否启用 AI 分析环节（true/false），在语音识别之后执行
AI_ENABLED=true

# ── 3.2  API 连接 ───────────────────────────────────────────────────────────
# 【自由】OpenAI 兼容 API 配置（⚠️ 请勿提交真实 API Key 到此文件）
AI_API_KEY=your-api-key-here
AI_BASE_URL=https://apihub.agnes-ai.com/v1
AI_MODEL=agnes-2.5-pro
# AI_MODEL=agnes-2.5-flash

# ── 3.3  推理参数 ───────────────────────────────────────────────────────────
# 【自由】AI 推理温度 (0.0~2.0, 越低越确定/保守, 越高越随机/创意)
AI_TEMPERATURE=0.3

# ── 3.4  提示词模板 ─────────────────────────────────────────────────────────
# 【关联】{content} 占位符会被识别文本替换，必须保留
# 【自由】提示词内容可随意修改
# 支持文件路径：值指向存在的文件时自动读取文件内容（try-file-first 策略）
# CLI 覆盖：--ai-prompt <text|path> 优先级最高（CLI > .env > 内置默认）
#
# 设计要点：
#   · 两步法：先语义修正 Whisper 同音/形近/术语错误，再提取关键词
#   · 只修正明显错误，不改变原文语义，不添加新内容
#   · 让模型统计中英文字符占比判定语言类型，阈值 60%
#   · 最多 30 个关键词，只提取文本中明确出现的，禁止联想扩展
#   · 中文在前、英文在后，统一逗号分隔
#   · 核心防错：中文关键词绝不翻译成英文，英文关键词绝不翻译成中文
#   · 完整版提示词见 data/prompts/sshk.md
#   · \n 转义：代码会自动把字面量 \n 转为真正的换行符（dotenv 不解析转义）
#
AI_PROMPT_TPL=你是生物医药多语言内容分析与ASR纠错专家。对以下视频转录文本提取搜索关键词。\n\n【第一步：语义修正与术语消歧】语音识别（Whisper）在处理专业内容时极易出错。请在理解上下文的基础上，激活生物医药专业词典进行以下修正（仅内部推理使用，不改变原文语义，不添加新内容）：\n- 同音/近音错字：如"冻存"误为"洞存"、"储存"误为"铸存"、"传代"误为"传带"、"复苏"误为"复舒"、"抗体"误为"康体"、"细胞株"误为"细胞珠"、"培养基"误为"培养鸡"、"质粒"误为"智力"、"表达量"误为"表大量"。\n- 形近字混淆：如"印迹"误为"印记"、"缓冲液"误为"缓冲夜"、"核酸"误为"核算"、"测序"误为"侧序"。\n- 英文缩写与发音误判：如将"PCR"误识为中文或乱码，将"CRISPR"误识为"克里斯普"，需根据上下文还原为标准英文缩写。\n边界约束：仅修正确实存在明显错误的词汇，保持原文行文逻辑不变。\n\n【第二步：关键词提取规则】基于修正后的文本，严格遵循以下语言判定与提取规则：\n\n【语言判定】\n- 统计文本中的中文字符数与英文字母数\n- 中文占比 > 60% → 按纯中文规则处理\n- 英文占比 > 60% → 按纯英文规则处理\n- 两者均不满足 → 按中英混合规则处理\n\n【纯中文内容】\n- 只提取文本中明确出现或直接体现的、具备实际搜索价值的关键词，数量适中，涵盖提供的内容即可，但是不能重复和凭空捏造、联想、扩展，用英文逗号分隔\n- 关键词必须全部是中文，绝对不能翻译成英文\n- 优先提取 2-8 字的专有名词、技术短语或核心概念\n- 过滤单字及无意义泛词（如"的""是""这个""一个"等）\n\n【纯英文内容】\n- 只提取文本中明确出现或直接体现的、具备实际搜索价值的关键词，数量适中，涵盖提供的内容即可，但是不能重复和凭空捏造、联想、扩展，用英文逗号分隔\n- 关键词必须全部是英文，绝对不能翻译成中文\n- 优先提取 2-8 词的专业术语、基因/蛋白名称或实验方法等\n- 过滤单字及无意义泛词（如"the""this""is""a"等）\n\n【中英混合内容】\n- 只提取文本中明确出现或直接体现的、具备实际搜索价值的关键词，数量适中，涵盖提供的内容即可，但是不能重复和凭空捏造、联想、扩展\n- 语种隔离原则：中文关键词必须是中文，英文关键词必须是英文，互不翻译且严禁中英混杂\n- 排序原则：中文关键词置于前，英文关键词置于后，统一用英文逗号分隔\n\n通用规则：严禁凭空联想、扩展或编造文本中未出现的概念，宁少勿多。最终只输出以英文逗号分隔的关键词列表，不包含任何解释性文字。这是内容：{content}


# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  四、外部工具路径（选设 — 通常无需修改）                                 ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# 【自由】可改为工具的绝对路径或自定义命令名
# yt-dlp 命令行工具
YTDLP=yt-dlp
# ffmpeg 转码工具
FFMPEG=ffmpeg
# ffprobe 媒体信息探测
FFPROBE=ffprobe


# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  五、语音识别（选设 — 默认本地 openai-whisper CLI + 中文优化）            ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# 四种后端模式，任选其一（按场景推荐）：
#   · local           = 本地 openai-whisper CLI       ← 离线可用，默认；英文/多语种通用，无额外依赖
#   · faster-whisper  = 本地 faster-whisper (CTranslate2) ← 速度 4x、内存减半；★英文/多语种大批量首选
#   · service         = 远程 whisper.cpp server       ← 需先启动服务；本机无显卡、走远程 GPU 服务器的轻客户端
#   · funasr          = 阿里 FunASR (中文 WER ~5%, 远低于 Whisper 的 15%) ← ★纯中文场景首选
#                       funasr 模式下通过 FUNASR_MODE 进一步选择 cli / service 子模式
#   语言速查（照这个配，不必逐个试）：
#     · 纯英文  → WHISPER_BACKEND=faster-whisper + WHISPER_MODEL=large-v3 + WHISPER_LANGUAGE=en
#     · 纯中文  → WHISPER_BACKEND=funasr + FUNASR_MODEL=paraformer-zh（funasr 用自身语言，与 WHISPER_LANGUAGE 无关）
#     · 中英混合→ WHISPER_BACKEND=faster-whisper + WHISPER_MODEL=large-v3，WHISPER_LANGUAGE 留空让模型逐条自动判
#

# ═══════════════════════ 共享参数（local + faster-whisper + service 通用）═════
# 【自由】"local"、"faster-whisper"、"service" 或 "funasr"
WHISPER_BACKEND=local

# 【自由】推理温度 (0.0~1.0, 越低越确定)
WHISPER_TEMPERATURE=0.0

# 【自由】温度增量 (fallback 时升温步长)
WHISPER_TEMPERATURE_INC=0.2

# 【自由】输出格式: txt/vtt/srt/tsv/json/all (服务端映射到 response_format)
WHISPER_OUTPUT_FORMAT=json

# ═══════════════════════ 服务模式（backend=service）═══════════════════════════
# 【自由】whisper.cpp server 地址
WHISPER_SERVICE=http://localhost:9588

# 【自由】ggml 模型路径，留空=使用服务端默认
WHISPER_SERVICE_MODEL=D:/Programs/memo-ai/models/ggml-medium.bin

# ═══════════════════════ 本地模式（backend=local，openai-whisper CLI 专用）════
# 【自由】任务: transcribe/translate
WHISPER_TASK=transcribe

# 【自由】模型: tiny/base/small/medium/large-v3/turbo
WHISPER_MODEL=large-v3

# 【自由】中文设 zh 避免繁体混入; 英文设 en ; 留空=自动检测
WHISPER_LANGUAGE=en

# 【自由】推理设备: cpu 或 cuda
WHISPER_DEVICE=cpu

# 【自由】模型下载目录，留空=~/.cache/whisper（或 $XDG_CACHE_HOME/whisper）
WHISPER_MODEL_DIR=

# 【自由】beam 宽度 (温度=0 时生效, 默认5; 可增至10 提升精度)
WHISPER_BEAM_SIZE=5

# 【自由】候选数 (温度>0 时生效, 默认5)
WHISPER_BEST_OF=5

# ── 初始提示词 (initial_prompt) ──
# 给第一段音频（约 30s）提供词汇提示，提升专有名词/术语的识别准确率。
# 格式: 用空格或逗号分隔的关键词/短语，语言与音频内容一致。
# 默认值已填充生物医学领域高频术语（细胞/免疫/分子/蛋白/实验技术等 90+ 词）。
# 如果内容涉及其他领域，参考以下示例替换：
#   · 技术演讲: API framework architecture deployment pipeline
#   · 产品宣讲: 产品 方案 客户 市场 数据 分析 报告 优化
# 留空 = 不使用初始提示。
# 支持文件路径：值指向存在的文件时自动读取文件内容（try-file-first 策略）
# CLI 覆盖：--whisper-initial-prompt <text|path> 优先级最高（CLI > .env > 内置默认）
WHISPER_INITIAL_PROMPT=细胞 培养 传代 冻存 复苏 消化 计数 接种 转染 表达 筛选 克隆 抗体 抗原 免疫 蛋白 纯化 分离 检测 试剂 培养基 血清 缓冲液 裂解 提取 沉淀 离心 电泳 印记 杂交 DNA RNA PCR qPCR 测序 引物 探针 载体 质粒 酶切 连接 转化 流式 荧光 标记 染色 固定 封闭 ELISA Western IF IHC FACS 免疫组化 免疫荧光 显微镜 共聚焦 微孔板 培养皿 培养瓶 超净台 生物安全柜 离心机 酶标仪 细胞株 细胞系 原代 干细胞 诱导 分化 增殖 凋亡 迁移 侵袭 自噬 炎症 氧化应激 肿瘤 因子 抑制剂 激活 信号通路 表达量 敲除 敲低 过表达 干扰 siRNA shRNA CRISPR 编辑 基因 分子 组织 器官 样本 实验 分析 储存

# ── 上下文传递 (condition_on_previous_text) ──
# True  = 当前片段解码时传入前一片段的识别文本作为上下文，提升连贯性。
#         适合: 短音频(<30分钟)、单人连贯语音、风格统一的内容
#         风险: 前段识别错误会"传染"到后续片段，可能累积偏移
# False = 每段独立解码，互不影响。
#         适合: 长音频(>30分钟)、多人交替、嘈杂环境、口音重的内容
#         代价: 片段间可能缺乏过渡自然度
# 推荐 False: 长短视频混合场景, 避免长视频错误累积; True 仅适合短音频(<30分钟)单人连贯语音
WHISPER_CONDITION_ON_PREV=False

# 【自由】fp16 推理 (CPU 必须设为 False, CUDA 可设为 True)
WHISPER_FP16=False

# 【自由】CPU 线程数 (0=自动, 可手动指定如 4/8/16)
WHISPER_THREADS=0

# ═══════════════════════ faster-whisper 模式专用（backend=faster-whisper）══════
# 以下参数仅在 WHISPER_BACKEND=faster-whisper 时生效，local/service 模式忽略。
# Python 版使用 faster_whisper Python API；Node.js 版使用 whisper-ctranslate2 CLI。

# 【自由】计算精度: int8(CPU最优/快/省内存) | float16(GPU推荐) | int8_float16 | default | auto
WHISPER_COMPUTE_TYPE=int8

# 【自由】启用 VAD 静音过滤(Silero VAD)，减少幻觉、加速推理
WHISPER_VAD_FILTER=True

# 【自由】VAD 灵敏度阈值 (0.0~1.0, 越高越严格/只保留高置信语音)
WHISPER_VAD_ONSET=0.5

# 【自由】CTranslate2 并行 worker 数 (1=单线程, 批处理场景可调大)
WHISPER_NUM_WORKERS=1

# ── Whisper 额外参数（用于扩展和自定义 whisper CLI 选项）──
# WHISPER_EXTRA_ARGS=--beam_size 5 --best_of 5 --no_speech_threshold 0.6
# 说明：shell 字符串格式，自动拆分为参数数组，追加到 whisper 命令末尾（最高优先级）
# 优先级：CLI --whisper-extra-args > .env WHISPER_EXTRA_ARGS > 无
# 自动去重：如果 WHISPER_EXTRA_ARGS 中的参数与已有参数同名，已有参数会被移除
#
#  以下为进阶参数，需要时取消注释：
# WHISPER_PATIENCE=1.0                     # beam 解码耐心值 (默认等价于常规 beam search)
# WHISPER_SUPPRESS_TOKENS=-1               # 抑制 token 列表 (-1=抑制特殊符号, 可自定义)
# WHISPER_CARRY_INITIAL_PROMPT=False       # 每帧都使用初始提示 (可能降低上下文有效性)
# WHISPER_COMPRESSION_RATIO_THRESHOLD=2.4  # 压缩比阈值 (越低越严格, 可能多切段)
# WHISPER_LOGPROB_THRESHOLD=-1.0           # 对数概率阈值 (越高越严格, 如 -0.5)
# WHISPER_NO_SPEECH_THRESHOLD=0.6          # 静音检测阈值 (越低越灵敏, 如 0.3)
# WHISPER_HALLUCINATION_SILENCE_THRESH=    # 静音幻觉阈值 (非空时启用幻觉抑制)
# WHISPER_VERBOSE=True                     # 打印进度 (调试时开启)

# ══════════════════════════════════════════════════════════════════════════════
#  模型     参数量    磁盘占用   内存占用    相对速度  中文WER   适用场景
#  ──────── ──────── ───────── ────────── ──────── ──────── ──────────────────
#  tiny       39M     ~75 MB    ~150 MB     10x      ~30%+   快速测试、实时流式
#                                                            转录、极低资源设备
#
#  base       74M    ~145 MB    ~290 MB      7x      ~25%    低资源环境、嵌入式
#                                                            设备、非关键场景
#
#  small     244M    ~465 MB    ~930 MB      4x      ~18%    平衡速度与精度、中
#                                                            等算力设备首选
#
#  medium    769M    ~1.5 GB    ~3.0 GB      2x      ~12%    较高精度需求、批量
#                                                            处理、内容审核
#
#  turbo     ~800M   ~1.5 GB    ~3.0 GB      1.5x    ~7-8%   最新默认模型(2025)
#                                                            精度接近 large-v3 速度更快
#
#  large-v3 1550M    ~3.0 GB    ~6.0 GB      1x      ~5-7%   最高精度、生产环境
#                                                            正式发布、学术研究
#
#  ── 选择建议 ──
#  · 日常批量处理 → medium：2 倍速度换取 12% 词错率，性价比最高
#  · 最终发布质量 → large-v3：中文 WER 仅 5-7%，繁体/方言/口音覆盖更好
#  · 低显存设备   → small：930 MB 内存即可运行，适合 4GB VRAM 设备
#  · 快速验证     → tiny/base：秒级响应，适合调试流程和参数
#  · 离线场景     → 使用 local 模式 + 本地模型文件
#
#  注：WER（词错率）为多语言测试集基准值，实际表现取决于音频质量和口音。
#      内存占用为 fp16 推理估算值，int8 量化后减半。
#

# ═══════════════════════ FunASR 模式专用（backend=funasr）════════════════════
# 以下参数仅在 WHISPER_BACKEND=funasr 时生效，local/faster-whisper/service 模式忽略。
# FunASR 专攻中文场景，WER ~5%（Whisper 中文 ~15%），集成 VAD + 标点 + 热词能力。
# 需先安装: pip install funasr modelscope (CLI 模式)
#           或:   pip install funasr vllm fastapi uvicorn python-multipart (服务模式, GPU 推荐)

# ════════════ FUNASR_MODE 子模式（cli / service）═══════════════════
# cli     = 本地 funasr CLI (auto_model.generate, 首次自动下载 ModelScope 模型, CPU 也能跑)
# service = 远程 funasr-server (OpenAI 兼容 API, GPU 推荐, 适合多机/多任务并发)

# 【调序】"cli" 或 "service"
FUNASR_MODE=cli

# ════════════ FUNASR_MODEL 选型 ══════════════════════════════════════════════
#  可用模型对比（FunASR 1.3.9，2026-05-29 发布）
#
#   模型名               参数量  磁盘   速度       语种   时间戳  情绪  事件  说话人  推荐场景
#   ─────────────────── ────── ───── ──────── ────── ────── ──── ──── ────── ─────────────────
#   paraformer-zh        220M  ~1GB   13×实时   zh/en  ✓     ✗    ✗    需cam++ 中文首选(高精)
#   SenseVoiceSmall      234M  ~1GB  170×实时  50+    ✗     ✓    ✓    需cam++ 多语种/实时
#   Fun-ASR-Nano         800M  ~3GB  加速(vLLM) 31    ✓     ✗    ✗    需cam++ 复杂上下文
#   paraformer-zh-streaming 220M ~1GB  流式      zh/en  ✓     ✗    ✗    需cam++ 实时字幕
#   Qwen3-ASR           1700M  ~7GB  较慢      52     ✗     ✗    ✗    需cam++ 多语种高质量
#   GLM-ASR-Nano        1500M  ~6GB  较慢      17     ✗     ✗    ✗    需cam++ 方言场景
#   Whisper-large-v3    1550M  ~3GB  1×        多      ✗     ✗    ✗    需cam++ 兼容 Whisper 用户
#   Whisper-large-v3-turbo 809M ~2GB  2-3×      多      ✗     ✗    ✗    需cam++ Whisper 加速
#
#   辅助模型（按需搭配主模型使用）:
#   fsmn-vad              0.4M 内置    实时     zh/en  -      -    -    -      VAD 静音检测
#   ct-punc              290M  ~1GB   -        zh/en  -      -    -    -      标点恢复
#   cam++                 7.2M 内置    实时     通用    -      -    -    ✓     说话人分离/确认
#   emotion2vec_plus_large 300M ~1GB  -         -     -      ✓    -    -      情感识别
#
#   默认推荐: paraformer-zh (中文精度最高) + fsmn-vad (内置) + ct-punc (标点)
#            多语种:           SenseVoiceSmall (170×实时, 50+ 语种, 内置情绪/事件)
#            实时/流式:        paraformer-zh-streaming
#            需要说话人:       paraformer-zh + cam++
#

# 【自由】主 ASR 模型名(见上表)
FUNASR_MODEL=paraformer-zh

# 【自由】VAD 模型(留空=用主模型内置VAD)  fsmn-vad
FUNASR_VAD_MODEL=fsmn-vad

# 【自由】标点恢复模型(留空=不恢复标点)  ct-punc
FUNASR_PUNC_MODEL=ct-punc

# 【自由】说话人模型(留空=不做说话人分离)  cam++
FUNASR_SPK_MODEL=

# 【自由】情感识别模型(留空=不做情感识别)  emotion2vec_plus_large
FUNASR_EMOTION_MODEL=

# ════════════ FUNASR 推理参数（cli 模式专用）══════════════════════
# 【自由】推理设备: cpu 或 cuda(GPU 强烈推荐)
FUNASR_DEVICE=cpu

# 【自由】int8 量化(节省 50% 内存, 精度略降; GPU 设为 False)
FUNASR_QUANTIZE=True

# 【自由】动态批处理音频秒数(60-600, 大文件=更大)
FUNASR_BATCH_SIZE_S=300

# 【自由】热词 空格分隔(如 "魔搭 ModelScope 通义千问"), 显著提升专有名词
FUNASR_HOTWORD=

# 【自由】主语言(中文 zh, SenseVoice 用 auto 可自动检测 50+ 语种)
FUNASR_LANGUAGE=zh

# ── VAD 参数（仅当使用 fsmn-vad 时生效）──
# 【自由】VAD 最大单段长度(ms, 0=不切分)
FUNASR_VAD_MAX_SEGMENT=20000

# ════════════ FunASR 服务模式（backend=funasr + FUNASR_MODE=service）═══════
# 启动 funasr-server（需先 pip install funasr vllm fastapi uvicorn python-multipart）:
#   funasr-server --device cuda --port 8899        # GPU 推荐
#   funasr-server --device cpu  --port 8899        # CPU 也能跑(较慢)
#   funasr-server --device cuda --port 8899 --enable-mcp  # 额外启用 MCP 协议
# 默认端口 8899，提供 OpenAI 兼容 API: POST {SERVICE_URL}/v1/audio/transcriptions

# 【自由】funasr-server 地址
FUNASR_SERVICE_URL=http://localhost:8899

# 【自由】服务侧加载的模型 ID(首次调用会触发 ModelScope 下载)
FUNASR_SERVICE_MODEL=iic/SenseVoiceSmall

# ── FunASR 额外参数（用于扩展和自定义 funasr 参数）──
# FUNASR_EXTRA_ARGS=--batch_size_s 600 --hotword 通义千问
# 说明：shell 字符串格式，自动拆分为参数数组，追加到 funasr 命令末尾（最高优先级）
# 优先级：CLI --funasr-extra-args > .env FUNASR_EXTRA_ARGS > 无
# 自动去重：如果 FUNASR_EXTRA_ARGS 中的参数与已有参数同名，已有参数会被移除
#
#  以下为进阶参数，需要时取消注释：
# FUNASR_MODEL_REVISION=v2.0.4                # 锁定主模型版本(防止 ModelScope 默认分支升级)
# FUNASR_VAD_REVISION=v2.0.4                  # 锁定 VAD 模型版本
# FUNASR_PUNC_REVISION=v2.0.4                 # 锁定标点模型版本
# FUNASR_MODEL_DIR=                           # 模型缓存目录(留空=~/.cache/modelscope/hub)
# FUNASR_THREADS=4                            # 推理线程数(0=自动, ONNX runtime 适用)
# FUNASR_VERBOSE=False                        # 打印 funasr 内部进度(调试时开启)


# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  五-B、OCR 抽帧识别（选设 — 处理无音轨 / 音轨文字与时长不符的视频）        ║
# ╚═══════════════════════════════════════════════════════════════════════════╝
# 适用场景：视频画面烧录了字幕（ASR 只处理音轨识别不到），或音轨是水印循环、
# 被 VAD 过滤成空文本。OCR 用 PaddleOCR 对视频抽帧识别画面文字，作为 ASR 的兜底补充。
# 核心原则：OCR 永远是兜底 —— 最终写入内容列的是「OCR 与 ASR 择优」后的文本，
#           OCR 字符数 ≥ ASR 且 置信度达标才采用 OCR，否则回退 ASR，绝不让噪声覆盖干净 ASR。
# 引擎：pip install paddlepaddle paddleocr（Apache 2.0，免费离线，百度源国内无墙）
# 模型默认位置（Windows）：C:\Users\{username}\.paddlex（paddleocr 3.x 的 PaddleX 缓存；2.x 为 .paddleocr）
#   改位置避 C 盘：设置环境变量 PADDLE_OCR_BASE_DIR（如 D:\AI_Models\PaddleOCR），脚本会自动透传

# OCR 模式：auto（默认，按需触发）/ always（全量跑 OCR）/ off（关闭）
OCR_MODE=auto
# OCR 引擎：当前仅 paddleocr
OCR_BACKEND=paddleocr
# OCR 语言：en（生物医学英文站）/ ch
OCR_LANG=en
# 场景切换抽帧阈值（越大抽得越少，仅抽画面显著变化帧）
OCR_SCENE_THRESH=0.3
# 抽帧保护上限（超过则自动放宽阈值重抽，不盲目跳帧丢文字；1-2h 长视频也完整处理）
OCR_MAX_FRAMES=2000
# 文本块置信度下限（低于此值的识别结果丢弃）
OCR_CONF_THRESH=0.6
# auto 触发阈值：每分钟有效字符低于此值才触发 OCR（专治"说一句+长时间等待"的实验视频，放宽误触发）
OCR_TRIGGER_CPM=2
# ocr 产物最短长度（字符），低于此值视为残缺、续跑会重做
OCR_MIN_CHARS=30
# 调用 ocr_frames.py 的 Python 解释器（默认 python；若 paddleocr 装在其他环境可改绝对路径）
# PYTHON_BIN=python
# PaddleOCR 模型目录（默认 C:\Users\{username}\.paddlex；C 盘紧张时改其他盘，如 D:\AI_Models\PaddleOCR）
# PADDLE_OCR_BASE_DIR=D:\AI_Models\PaddleOCR

# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  六、转码参数（选设 — whisper 优化，通常无需修改）                       ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# 输出音频格式
TRANSCODE_EXT=.wav

# ↓ 完整转码参数链（按流水线顺序）
TRANSCODE_ARGS=-vn -map_metadata -1 -map 0:a:0 -af loudnorm=I=-16:TP=-1.5:LRA=11:linear=true,aresample=resampler=soxr:osr=16000:osf=s16:dither_method=shibata -ac 1 -c:a pcm_s16le

# ↓ 参数详解（按执行顺序）:
#
#   -vn                跳过视频流 → 仅处理音频，大幅加速
#   -map_metadata -1   剥离所有元数据 → 减小文件体积
#   -map 0:a:0         只取第一个音频流 → 避免多音轨视频选错轨道
#   -af "..."          音频滤镜链（见下方逐项说明）
#   -ac 1              输出单声道 → whisper 只需单声道
#   -c:a pcm_s16le     编码器: WAV PCM 16-bit signed little-endian
#
# ↓ 音频滤镜链详解（-af 参数，从左到右依次执行）:
#
#   ① loudnorm=I=-16:TP=-1.5:LRA=11:linear=true
#      EBU R128 响度归一化（单次线性模式）:
#      · I=-16   → 目标响度 -16 LUFS（广播标准，whisper 最优输入电平）
#      · TP=-1.5 → 真峰值限制 -1.5 dBTP（防止削波失真）
#      · LRA=11  → 响度范围 11 LU（典型语音动态范围）
#      · linear=true → 线性归一化，单次处理无需预扫描，快速且适合语音
#
#   ② aresample=resampler=soxr:osr=16000:osf=s16:dither_method=shibata
#      soxr 高质量重采样器:
#      · resampler=soxr       → 比 ffmpeg 默认 swr 精度更高，抗混叠更优
#      · osr=16000            → 输出采样率 16kHz（whisper 原生采样率）
#      · osf=s16              → 输出 16-bit signed integer
#      · dither_method=shibata → 芝畑抖动算法，24/32-bit→16-bit 量化时消除截断失真
#

# ═══════════════════════ 备选方案 ═══════════════════════
# 如果不需要响度归一化，改为下面这行:
# TRANSCODE_ARGS=-vn -map_metadata -1 -map 0:a:0 -af aresample=resampler=soxr:osr=16000:osf=s16:dither_method=shibata -ac 1 -c:a pcm_s16le
