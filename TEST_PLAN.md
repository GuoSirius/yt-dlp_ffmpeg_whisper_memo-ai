# 全面测试计划 - process_videos.js & process_videos.py

## 测试范围
- 两个版本（Node.js 和 Python）的所有功能
- 边界情况
- 安全性审查
- 错误处理
- 性能考虑

## 已发现的潜在问题

### 🔴 高优先级（安全性）

#### 1. 路径遍历漏洞（两个版本）
- **位置**: `safeFilename()` / `safe_filename()` 函数
- **问题**: 只过滤了 `[\/:*?"<>|]`，未过滤 `..` 和 `.`
- **影响**: 如果 Excel 中的 ID/标题包含 `..`，可能导致文件写入意外位置
- **测试**: 在 Excel 中插入包含 `..` 的 ID，检查文件是否被正确清理

#### 2. 命令注入（低风险，因为使用 spawn 而非 exec）
- **位置**: `spawnWithTimeout()` / `run_with_progress()`
- **状态**: 使用 `spawn`（不直接调用 shell），风险较低
- **但需验证**: 所有参数都正确转义

### 🟡 中优先级（健壮性）

#### 3. 重试逻辑边界情况
- **位置**: `retryCall()` / `retry_call()`
- **测试**: maxRetries=0, 网络超时, 非重试错误（404, 403）

#### 4. Excel 写入并发冲突
- **位置**: `writeExcelCell()` / `write_all_contents_to_excel()`
- **问题**: 没有文件锁定，多并发可能冲突
- **测试**: 并发写入同一 Excel 文件

#### 5. 大文件处理
- **测试**: 超过 2GB 的视频文件
- **测试**: Excel 文件包含 10,000+ 行

### 🟢 低优先级（代码质量）

#### 6. 硬编码值
- WHISPER_SERVICE 默认端口 9588
- 需要确认为 intentional

#### 7. 日志敏感信息
- 检查是否记录 API Key 或 Cookie

---

## 测试执行计划

### 阶段 1: 静态分析（已完成 20%）
- [x] 函数列表提取
- [ ] 安全性审查
- [ ] 错误处理审查
- [ ] 边界情况审查

### 阶段 2: 单元测试（待执行）
- [ ] 测试 `safeFilename()` / `safe_filename()`
- [ ] 测试 `parseUrl()` / `parse_url()`
- [ ] 测试 `retryCall()` / `retry_call()`
- [ ] 测试 `computeSummary()` / `compute_summary()`

### 阶段 3: 集成测试（待执行）
- [ ] 测试 --help 输出
- [ ] 测试 --dry-run 模式
- [ ] 测试 --step 参数组合
- [ ] 测试 --concurrency 参数
- [ ] 测试 --retry 和 --retry-delay

### 阶段 4: 边界情况测试（待执行）
- [ ] 空 Excel 文件
- [ ] 无效视频 ID
- [ ] 网络错误模拟
- [ ] 文件权限错误
- [ ] 磁盘空间不足

### 阶段 5: 报告生成（待执行）
- [ ] 验证 JSON 报告格式
- [ ] 验证控制台输出格式

---

## 当前进度
- 代码审查: 20% 完成
- 功能测试: 0% 完成
- 边界情况测试: 0% 完成
- 测试报告: 未生成

