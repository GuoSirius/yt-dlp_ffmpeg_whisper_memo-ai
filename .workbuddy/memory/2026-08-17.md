# 2026-08-17

## 新增 COL_VIDEOLINK 视频链接列优先解析（双端 process_videos.js / .py 对称）
- 新增配置 `COL_VIDEOLINK`（默认 `extra.videoLink`）：单元格放**完整视频链接**（标准观看页/短链/内嵌 iframe），下载时优先解析，自动识别平台+提取 ID 拼标准 URL。
- 优先级：① 链接列（parseUrl 识别）→ ② 四个显式 ID 列（COL_TENCENTVID/...）→ ③ 原流程（no_video/告警）。链接为空或未知平台时回退到显式 ID 列。
- 未知平台链接：控制台 `styleWarn` 告警 + 聚合，运行结束写入 `output/logs/unknown-platform-links.log`（作为后续新增平台依据）。
- 顺带修复 parseUrl：Tencent `[?&]vid=` 提到 page 模式之前（避免 `x.html?vid=...` 被 page 抢成占位 `x`）；Bilibili 新增 `player.bilibili.com/...bvid=` 内嵌页解析。
- 已同步全局副本（sync_global_copy.mjs，CRLF→LF），commit `9789335` 推送 gitee main。

## 补全视频链接列支持的平台 URL 格式（对齐 README 9.1）
- 新增解析：B站 `av` 号（`av\d+`，含前缀）、B站移动端 `m.bilibili.com`、优酷 `/video/` 路径、优酷 `www.youku.com` 域名。腾讯/YouTube 格式已覆盖 9.1 全部条目。
- 回退逻辑（上轮已正确）不变：链接为空 / 列缺失 / 未提取到有效 ID / 平台不在四者中 → 一律回退四列显式 ID，未知平台链接继续告警并落盘 `unknown-platform-links.log`。
- 双端 20 条 README 9.1 格式用例全过；同步全局副本，commit `a1b16bc` 推送 gitee main。
