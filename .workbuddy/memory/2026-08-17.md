# 2026-08-17

## 新增 COL_VIDEOLINK 视频链接列优先解析（双端 process_videos.js / .py 对称）
- 新增配置 `COL_VIDEOLINK`（默认 `extra.videoLink`）：单元格放**完整视频链接**（标准观看页/短链/内嵌 iframe），下载时优先解析，自动识别平台+提取 ID 拼标准 URL。
- 优先级：① 链接列（parseUrl 识别）→ ② 四个显式 ID 列（COL_TENCENTVID/...）→ ③ 原流程（no_video/告警）。链接为空或未知平台时回退到显式 ID 列。
- 未知平台链接：控制台 `styleWarn` 告警 + 聚合，运行结束写入 `output/logs/unknown-platform-links.log`（作为后续新增平台依据）。
- 顺带修复 parseUrl：Tencent `[?&]vid=` 提到 page 模式之前（避免 `x.html?vid=...` 被 page 抢成占位 `x`）；Bilibili 新增 `player.bilibili.com/...bvid=` 内嵌页解析。
- 已同步全局副本（sync_global_copy.mjs，CRLF→LF），commit `9789335` 推送 gitee main。

## 补全视频链接列支持的平台 URL 格式（对齐 README 9.1）
- 新增解析：B站 `av` 号（`av\d+`，含前缀）、B站移动端 `m.bilibili.com`、优酷 `/video/` 路径、优酷 `www.youku.com` 域名。腾讯/YouTube 格式已覆盖 9.1 全部条目。
- 回退逻辑（上轮已正确）不变：链接为空 / 列缺失 / 未提取到有效 ID / 平台不在四者中 → 一律回退四列显式 ID，未知平台链接继续告警并落盘 `unknown-platform-links.log`。
- 双端 20 条 README 9.1 格式用例全过；同步全局副本，commit `a1b16bc` 推送 gitee main。

## 补 .env.example 的 COL_VIDEOLINK（用户指出疏漏）
- 用户发现 `.env`/`.env.example` 四个平台 ID 列齐全但缺新增的 `COL_VIDEOLINK`。代码有默认值故能跑，但 env 未声明。
- 在 `.env`（本地，gitignore 不提交）与 `.env.example`（提交，commit `f0547b7`）的「平台视频 ID 列」区块后补 `COL_VIDEOLINK=extra.videoLink` 及说明注释。
- 验证：双端均用 dotenv 加载 `.env`（JS `dotenv.config` / Py `load_dotenv`），新增行会被正常读取。
- 铁律提醒：`.env` 含真实 API Key 已被 `.gitignore` 排除，提交时只动 `.env.example`，绝不把 `.env` 纳入。

## 配置三处一致性审计 + REFERRER 拼写 bug 修复
- 用户要求把「代码实际读取 / .env.example / README」三处配置统一归纳对比。结论：无硬遗漏（生效项三处都有），但发现 **REFERRER 拼写 bug**：代码读单 R `${prefix}_REFERER`（JS:393 / PY:359），`.env`/`.env.example` 却写双 R `BILIBILI_REFERRER`，导致 B 站 Referer 防盗链从未生效（易 403）。
- 用户裁决：WHISPER_LANGUAGE（默认 en 有意）、14 个「进阶」未接入变量（仅作 env 说明不动）、WHISPER_SERVICE_MODEL（本机路径不动）、EXCEL_FILE（样例名不一致不动）均保持不变；仅「补齐 README §3.1 漏列的 `{平台}_REFERER` / `{平台}_CONCURRENT_FRAGMENTS`」。
- 实际修复：把 `.env`+`.env.example` 的 `BILIBILI_REFERRER` 改单 R `BILIBILI_REFERER`（含注释 `{前缀}_REFERRER`→`{前缀}_REFERER`），并在 README §3.1 补两行。审计报告 `docs/config-audit-20260817.md`。commit `df3db24` 推送 gitee main。

## 标注 14 个「进阶未读」变量为调参说明
- 用户要求给 README §3.1 那 14 个进阶变量加注「当前版本由 *_EXTRA_ARGS 传入」。经核查这些变量实际只存在于 `.env`/`.env.example` 的「以下为进阶参数，需要时取消注释：」区块（WHISPER 系 + FUNASR 系，共 14 个），README §3.1 仅以 `WHISPER_*`/`FUNASR_*` 汇总行提及。
- 已在三处加注：`.env.example`、`.env`（两处进阶区块各加 ⚠️ 行）、README §3.1 汇总行补一句「进阶调参项代码不单独读取，须经 `*_EXTRA_ARGS` 传入」。`.env` 为 gitignore 本地副本，已同步改但仅提交 `.env.example`+README。commit `74c1d96` 推送 gitee main。
