## 2026-06-15

### 增量 JSON 进度保存（方案 C）
- 实现：完成任务后立即写 `output/progress/{sheet}/task_{stem}.json`
- Python: `save_task_progress()` + 在 `run()`/`run_from_report()` 的 `as_completed` 回调中调用
- Node.js: `saveTaskProgress()` + 在 `run()`/`runFromReport()` 的 `p-limit` 回调中调用
- 每个 task JSON 包含：content(识别文本)、keywords(AI分析)、各步骤status、timestamp
- 终态 Excel 回写保持不变（全部完成后批量写）
- 线程安全：每任务独立文件，天然无冲突

### 增量进度保存修复（3 项）
1. `.env.example` 补充 `PROGRESS_DIR=output/progress`
2. 内存优化：`save_task_progress()` 写 JSON 后立即释放 `transcribe.file`/`analyze.file`（设为 None/null）
   - `run()`/`runFromReport()` 回调中提前收集 content/keywords 到独立 dict（Python: content_updates/ai_updates, JS: contentMap/kwMap）
   - `write_all_contents_to_excel` 新增 `content_dict` 参数，优先使用预收集的 dict
3. 日志路径动态化：`os.path.relpath(progress_file, BASE_DIR)` / `path.relative(BASE_DIR, progressFile)` 替代硬编码
