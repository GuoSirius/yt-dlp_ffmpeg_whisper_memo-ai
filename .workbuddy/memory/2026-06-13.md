# 2026-06-13

## 任务12: 提示词支持文件路径、CLI 参数、whisper extra-args (20:03~20:20)

### 需求
1. WHISPER_INITIAL_PROMPT 和 AI_PROMPT_TPL 支持文件路径（try-file-first 自动探测）
2. 新增 CLI 参数 `--whisper-initial-prompt`、`--ai-prompt`、`--whisper-extra-args`
3. WHISPER_EXTRA_ARGS 支持用户自定义 whisper 扩展参数，自动去重合并

### 实现
- `resolvePromptValue(val)`: 检测文件存在则读取内容，否则当文本；自动处理 \n \t 转义
- `parseExtraArgs(str)`: shell 字符串拆分为参数数组
- `mergeWhisperArgs(base, extra)`: 同名 key 去重，extra 最高优先级
- `applyCliOverrides(opts)`: CLI 解析后统一应用覆盖，优先级 CLI > .env > 内置默认

### 文件变更
- `process_videos.js`: 新增工具函数 + applyCliOverrides + 三个 CLI option + mergeWhisperArgs 集成
- `process_videos.py`: 同步实现（Python 版本）
- `.env.example`: 新增 WHISPER_EXTRA_ARGS 说明和文件路径支持说明

### 提交
- `973c80b`: feat: 提示词支持文件路径、新增 CLI 参数、whisper extra-args 去重
