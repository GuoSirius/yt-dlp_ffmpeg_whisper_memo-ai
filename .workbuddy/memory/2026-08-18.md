# 2026-08-18

- Excel 实时写回「占用等待」增加心跳计时提示（双端 `process_videos.js` + `process_videos.py` 同改）。首次检测到占用仍立即告警一次；之后每 10s 打印一次「⏳ Excel 仍被占用，已等待 Xs，继续重试」心跳；`finalizeExcel/finalize_excel` 收尾时打印「📝 全部任务完成，正在将结果写回 Excel...」。解决“等待写入时全程无声、用户以为卡死”的体验问题。已 `node --check` + `py_compile` 通过、`sync_global_copy.mjs` 同步全局副本、commit `c4f830c` 推 gitee+github。
- 踩坑：commit subject 行首为英文大写词「Excel...」触发 commitlint `subject-case` 拒收，改以中文开头「fix: 增加 Excel...」通过（与 MEMORY.md 既有坑一致）。
- 根因确诊：`export-topics.xlsx`（采集数据 sheet / H 列 extra.content）含 **16 个 >32767 字符**的历史单元格（H48/49/70/96/101/107/109/118/126/130/131/133/138/141/148/154，最长 H130=62283）。SheetJS 每次 `XLSX.writeFile` 校验全部单元格 → 抛 `Text length must not exceed 32767 characters`，原代码 catch-all 误报成「被占用」。只读扫描 + 临时副本重存盘均验证属实，原文件本身就有问题，与是否被 Excel 打开无关。
- 决议（用户选定「导出 sidecar 保留全量」）：落盘前统一扫描超限单元格，完整内容导出到同名 `*.fulltext.json`（sidecar），主表该格截断到 32767 内并加「…(全文N字已导出至 sidecar)」标注。两端（`_exportOversizedToSidecar` / `_export_oversized_to_sidecar`）在 `_trySaveWb` / `_try_save_excel` 调用；同时移除写入路径里原先的静默截断（保留全量）。这样既不误报「被占用」、也不丢内容。非破坏性端到端测试通过（16 格导出、重存盘 SAVE_OK）。已 `node --check`+`py_compile`、commit `aaf8853` 推 gitee+github、`sync_global_copy.mjs` 同步全局副本。
- 注意：write 路径原先对「新写入」也有 32767 静默截断（丢尾），本次一并移除并改由落盘侧统一导 sidecar；若日后发现新写内容异常变短，回头查这里。
