# 2026-06-10 工作日志

## Excel 拆分任务

- 输入文件：`export_2026-06-10.xlsx`（104 行，17 列）
- 输出文件：`export_2026-06-10_split.xlsx`，包含三个 sheet：
  - **PDF资源**：`extra.pdfUrl` 有值，共 13 条
  - **YouTube视频**：`extra.youtubeId` 有值，共 19 条
  - **其他**：两者均无值，共 72 条
- 三类互不重叠，总计 104 条完整覆盖
- 后续补充需求：各 sheet 中有 `extra.id` 的行按正序排序（id 无值的行排在末尾）
  - PDF资源：无 extra.id 值（全为 NaN），顺序不变
  - YouTube视频：19 条均有 extra.id，按正序排列
  - 其他（普诺赛中文站）：68 条有 extra.id 按正序，4 条无值排末尾

## 视频下载/转码/识别流程搭建

- 生成 cookie 文件：cookies/bilibili.txt（20 条）、cookies/youtube.txt（7 条），Netscape 格式
- 目录结构：downloads/{sheet名}、transcoded/{sheet名}、cookies/
- yt-dlp 路径：`C:\Users\Admin\AppData\Local\Programs\Memo\resources\yt-dlp\yt-dlp.exe`
- ffmpeg 路径：`C:\Users\Admin\AppData\Local\Programs\Memo\resources\addon\ffmpeg\ffmpeg.exe`
- whisper 服务：http://localhost:9588（/inference 识别，/load 选模型）
- 转码格式：WAV 16kHz mono（pcm_s16le），对 whisper 识别效果最佳
- 脚本：`process_videos.py`，支持全量/指定 sheet/指定 id/指定 step/--force 参数
- **并发 + 重试 + 报告增强**（14:18）：
  - `--concurrency N`：多线程并发下载/转码（ThreadPoolExecutor），默认 1，建议 2~3
  - `--retry N`：每个步骤失败后最大重试次数，默认 0
  - `--retry-delay S`：重试间隔基数（秒），指数退避 5→10→20→40...
  - 智能重试：网络错误/超时可重试，404/403/视频已删除等永久错误不重试
  - `--dry-run`：干跑模式，仅列出任务清单及已有文件状态
  - `--retry-failed report.json`：从历史报告加载失败项重新执行
  - 报告机制：`reports/report_TIMESTAMP.json`，含 per-video 详细状态、错误信息、摘要统计
  - Excel 批量写回：所有识别文本统一收集后单线程写入，避免 openpyxl 线程安全问题
  - 故障排查：控制台打印失败/异常详情，报告 JSON 可直接用于 `--retry-failed` 重跑
- 数据量：YouTube视频 19 条 + 普诺赛中文站 72 条 = 91 条视频任务
- **总体进度追踪**（14:32）：
  - 每个任务启动时打印 `[1/91] [stem]` 标签，显示当前位置
  - 每个任务完成后打印 `[总进度 N/91 (X.X%)] ✅成功 ❌失败 ⚠️部分 ⏭️无视频`
  - 使用 `OverallProgress` 数据类 + Lock 实现线程安全计数
  - 同时覆盖 `run()` 和 `run_from_report()` 两条执行路径
  - README 新增"进度显示"章节，展示两层级进度输出效果
- **B站 412 错误修复**（14:50）：
  - 根因：B站反爬要求 Chrome UA + Referer 头，且 cookie 需包含有效 SESSDATA/bili_ticket
  - 修复：`PLATFORM_CONFIG.bilibiliBvid` 增加 `extra_headers`（UA/Referer/Accept-Language）
  - Cookie：用用户浏览器最新 cookie 覆盖 `cookies/bilibili.txt`（24 条，Netscape 格式）
  - 格式优化：B站视频选 720p（`bestvideo[height<=720]`），音频码率不变但下载快 3-5 倍
  - 并发分片：`--concurrent-fragments 4` 加速音视频流并行下载
  - 验证：id=247/248/249 三条 B站视频全部成功下载（用时 27-63s）
- **YouTube 下载修复**（15:16）：
  - 问题：`Sign in to confirm you're not a bot` + n-sig JS 挑战失败
  - 根因：cookie 过期 + 缺少 JS 运行时解 n-sig 挑战 + 缺少 Chrome UA
  - 修复1：用户通过 Chrome 扩展"Get cookies.txt LOCALLY"导出新 cookie 覆盖 `cookies/youtube.txt`（26 条）
  - 修复2：`PLATFORM_CONFIG.youtubeId` 增加 `extra_headers`（Chrome UA + Accept-Language）和 `extra_args`（`--js-runtimes node`）
  - 修复3：`download_video()` 新增 `extra_args` 处理逻辑，与 `extra_headers` 并列
  - 测试：`wGR1OmaCFBU`（Proper Cryopreservation Techniques Part 1）下载成功，18MB mkv，9s 完成
  - 注意事项：n-sig 挑战偶尔网络超时，建议脚本重试机制覆盖
- **工具路径移植**（15:21）：
  - `YTDLP`/`FFMPEG` 绝对路径 → `"yt-dlp"`/`"ffmpeg"`（依赖 PATH），方便跨电脑使用
  - 新增 `FFPROBE = "ffprobe"`，`get_duration()` 改用 ffprobe 直接取时长（不再解析 stderr 正则）
  - 移除 `--ffmpeg-location` 参数，yt-dlp 从 PATH 自动找 ffmpeg
  - README 全面重写：新增必装工具表格、Node.js/Deno JS 运行时说明、Cookie 导出步骤、平台适配对比表、常见错误排查表、换电脑使用指南
- **步骤级超时中断机制**（15:27）：
  - 新增 `--download-timeout`（默认 600s）、`--transcode-timeout`（默认 600s）、`--transcribe-timeout`（默认 600s）三个 CLI 参数
  - 超时后自动 kill 子进程 → 标记失败 → 继续后续任务，不阻塞并发队列
  - `subprocess.TimeoutExpired` 属于 `RETRYABLE_ERRORS`，触发指数退避重试
  - 超时参数穿透完整调用链：`run()`/`run_from_report()` → `process_one_task()` → 各 step 函数 → `run_with_progress()` / `requests.post()`
  - README 更新：新增"超时控制"章节、重试规则表新增步骤级超时、参数表新增三个 timeout 参数
- **Git 提交推送**（15:32）：commit `24b178d`，推送至 Gitee `origin/main`。提交范围：process_videos.py / README.md / Excel 文件。cookies/youtube.txt 和 .workbuddy/memory 未入库（凭据/本地日志）
- **run_with_progress 同步化**（15:50）：
  - 线程读取 stderr → 同步读取 stdout（stderr 合并到 STDOUT），消除线程竞态
  - yt-dlp 添加 `--newline` 确保进度行以 `\n` 结尾逐行可读
  - 总进度 emoji → 纯文本标签（兼容性更好）
  - 每行带 `[{label}] L{N}:` DEBUG 前缀，方便排查解析问题
- **智能重转码**（16:14）：
  - `step_transcode` 增加 mtime 比对：MP4 修改时间 > WAV 修改时间时自动重转码
  - 无需 `--force`，重新下载过的视频自动触发转码更新，确保音画一致
  - README 新增「智能跳过与自动重转码」章节
- **Git 提交**（16:21）：commit `375a9d2`，含 run_with_progress 同步化 + 智能重转码 + 文档更新
