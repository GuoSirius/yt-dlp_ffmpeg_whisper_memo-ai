# 2026-06-10 工作日志

## YouTube 下载问题最终解决

### 问题根因演进
1. 原始问题：`Sign in to confirm you're not a bot` → YouTube 反爬
2. 第一轮修复：`--cookies-from-browser chrome` → Chrome DPAPI 解密失败（Windows 已知问题 yt-dlp #10927）
3. 第二轮修复：手动导出 cookie 文件 → cookie 48h 过期
4. **最终方案**：`--cookies-from-browser firefox` + yt-dlp 2026.06.09

### 今日操作
- ✅ 升级 yt-dlp: 2026.03.17 → 2026.06.09
- ✅ 验证 Firefox cookie 方案：测试视频 `zzJmKPX8a3c` 成功获取元数据 + 下载链接
- ✅ `.env` 切换为 `YOUTUBE_COOKIES_FROM_BROWSER=firefox`（替代 `YOUTUBE_COOKIE_FILE`）
- ✅ `.env.example` 更新：推荐 Firefox 方案，保留 cookie file 作为备用，更新注释
- ✅ dry-run 验证：91 条任务全部正常加载
- 提交：63dca08 (代理端口), 3230df0 (Firefox cookie 方案)

### 关键发现
- yt-dlp 2026.06.09 大幅改进了 YouTube 提取，某些情况下不再需要 JS runtime 解决 n-sig
- Firefox 的 cookie 存储格式能被 yt-dlp 稳定解密（不受 DPAPI 问题影响）
- Firefox 方案是**可持续方案**：cookie 随浏览器登录态自动维护，无 48h 过期问题

### 运行前提条件
1. Firefox 浏览器需登录 YouTube
2. Clash Verge 代理运行中（端口 7897）
3. whisper.cpp server 运行中（端口 9588）
4. 执行命令：`python process_videos.py`

---

## 端到端验证通过 (22:02 - 22:05)

测试视频：`zzJmKPX8a3c` (YouTube视频/extra.id=2143)

### 流水线耗时
| 步骤 | 耗时 | 输出 |
|------|------|------|
| 下载 | 31s | 33MB, 720p mp4 |
| 转码 | 1s | 11.5MB, 16kHz mono WAV |
| 识别 | 24s | 6280字符 (whisper base) |
| 写入Excel | 1s | ✅ |

### 遇到的问题
- **n-sig 求解失败**：yt-dlp 需 `--js-runtimes node --remote-components ejs:github`，脚本已通过 `.env` 的 `YOUTUBE_JS_RUNTIMES` / `YOUTUBE_REMOTE_COMPONENTS` 配置（`extra_args` → 自动注入）
- **Excel 写入权限**：文件被 Excel 打开时 openpyxl 无法保存 → 提示用户关闭后重试

### 识别质量
英文视频，whisper base 模型，识别质量优秀，标点准确，无显著错误。

### 数据提交
`d8ff231` - data: YouTube视频 zzJmKPX8a3c(2143) 语音识别结果写入

---

## 残留文件清理 + 文档更新 (22:05 - 22:15)

### 修复清理逻辑
- 问题：`downloads/` 目录发现 `2152.mp4.part` 和 `2152.mp4.ytdl` 残留，脚本无清理机制
- 修复：新增 `_cleanup_partial_files()` 函数，在 3 个时机自动清理：
  1. 下载开始前 — 清除上次中断残留
  2. 跳过已有文件时 — 检查并清除历史残留
  3. 下载失败后 — 立即清理
- 提交：`0251a4b`

### README 文档全面更新
- 环境变量表：新增 `COOKIES_FROM_BROWSER`、`PROXY`、`JS_RUNTIMES`、`REMOTE_COMPONENTS`
- Cookie 设置：Chrome 扩展方案 → Firefox 直读（推荐）+ 手动导出（备用）
- 平台适配表：补充代理说明
- 常见下载错误：新增 cookie 过期、代理问题、DPAPI 失败等 8 项
- 换电脑指南：更新为 Firefox 方案
- 临时文件清理：新增章节

### .gitignore 安全加固
- 取消注释 `cookies/`，防止 bilibili.txt / youtube.txt 泄露
- `git rm --cached` 移除已跟踪的 cookie 文件（本地保留）

### 提交
- `a90b5af` - docs: 文档全面更新 + security: cookie 文件移出版本控制
- 全部 15 个 commit 已推送至 gitee (57d329b..a90b5af)
