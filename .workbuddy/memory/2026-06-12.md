# 2026-06-12

## 任务1: 排查语音识别质量问题（错字/漏字/繁体）

### 根因分析
1. `/inference` 缺少 `language=zh` → 繁体字混入 + 错字（用户自行在服务后台设定）
2. medium 模型精度不足（中文 WER ~12%）→ 错字/漏字
3. ffmpeg 转码参数简陋：
   - `-ac 1` 简单混音存在相位抵消风险
   - 未指定 resampler（默认 swr）
   - 无响度归一化
   - 无抖动处理（24/32→16bit 截断失真）

→ 用户决定：language 自己设定、转码参数优化、模型自己选

## 任务2: 优化 ffmpeg 转码参数

### 新 TRANSCODE_ARGS
```
-vn -map_metadata -1 -map 0:a:0 -af loudnorm=I=-16:TP=-1.5:LRA=11:linear=true,aresample=resampler=soxr:osr=16000:osf=s16:dither_method=shibata -ac 1 -c:a pcm_s16le
```

### 改动文件
- process_videos.js: 更新默认 TRANSCODE_ARGS
- process_videos.py: 更新默认 TRANSCODE_ARGS
- .env.example: 替换旧 TRANSCODE_ARGS 段，逐项详解
- .env: 同步更新（未提交，gitignored）

### 各参数作用
| 参数 | 作用 |
|---|---|
| -vn | 跳过视频流，加速 |
| -map_metadata -1 | 剥离元数据 |
| -map 0:a:0 | 只取第一音频流 |
| loudnorm | EBU R128 响度归一化到 -16 LUFS |
| aresample=soxr | 高质量重采样器（比 swr 精度更高） |
| dither_method=shibata | 芝畑抖动消除量化失真 |
| -ac 1 | 单声道 |
| -c:a pcm_s16le | WAV PCM 16-bit |

### 验证
- 合成音频测试 OK
- 真实视频转码 OK（pcm_s16le, s16, 16000Hz, 1ch, 376.2s）
- JS/Python 语法检查通过

## 任务3: Whisper 模型对比文档

在 .env.example 中新增详细对比表：

| 模型 | 参数量 | 磁盘 | 内存 | 速度 | 中文 WER | 场景 |
|------|--------|------|------|------|----------|------|
| tiny | 39M | 75MB | 150MB | 10x | ~30%+ | 快速测试 |
| base | 74M | 145MB | 290MB | 7x | ~25% | 低资源 |
| small | 244M | 465MB | 930MB | 4x | ~18% | 平衡 |
| medium | 769M | 1.5GB | 3GB | 2x | ~12% | 批量 |
| large-v3 | 1550M | 3GB | 6GB | 1x | ~5-7% | 生产 |

选择建议：批量用 medium，最终质量用 large-v3。

### Git
- commit d777654: feat: 优化 ffmpeg 转码参数 + 补充 Whisper 模型对比文档

## 任务4: 重新整理 .env.example 配置结构

按照必设优先级和功能关联关系重新分组为 6 大块：
一、输入与字段映射（必设）、二、平台与下载（必设）、三、AI关键词分析（必设）、
四、外部工具路径（选设）、五、语音识别（选设）、六、转码参数（选设）

新增编号导航（1.1~6.x），所有注释完整保留，补充了离线场景建议。
同步整理 .env，保持实际配置值不变仅调整结构。

## 任务5: 调整超时默认值 + 实现 0=不超时

### 新默认值
| 环节 | 旧值 | 新值 |
|------|------|------|
| 下载 | 600s | 1800s (30min) |
| 转码 | 600s | 1200s (20min) |
| 识别 | 1200s | 0 (不限制) |
| AI分析 | 300s | 300s (5min) |

### 0=不超时逻辑
- JS: spawnWithTimeout/stepTranscode/stepAnalyze/transcribeService 中 setTimeout 条件化（timeout>0 才设置定时器）
- PY: run_with_progress 超时检测条件化；subprocess.run/requests/urllib 的 timeout 参数为 0 时传 None
- CLI help 文本更新，注明"设为 0 则不限制"

### 改动文件
- process_videos.js: CLI defaults + 6 个函数签名 + 4 处条件化 setTimeout
- process_videos.py: argparse defaults + 6 个函数签名 + 4 处条件化 timeout
- README.md: 示例 + 默认值说明 + 参数表
- .env.example / .env: 无超时配置（超时为 CLI 参数）

## 任务6: 本地 Git 提交

- commit 34d08e4: feat: 新增控制台进度显示模块 + 优化转码参数 + 调整超时默认值
- 6 files changed: .env.example, README.md, process_videos.js, process_videos.py, console-ui.mjs (new), console_ui.py (new)
- 未推送，由用户自行 git push

## 任务7: 修复 AI 分析失败时错误信息为 null 的 Bug

### 根因分析（3 个问题叠加）
1. **retryCall 返回值嵌套结构不匹配**：retryCall 返回 `{result, retriesUsed, error}`，但 stepAnalyze 按 `{text, retries, error}` 解构 → `result.text` 拿到的是嵌套对象（永远 truthy），`result.error` 来自 retryCall 顶层（永远是 null）
2. **isRetryable 对 API HTTP 5xx 错误不重试**：fetch 抛出的 HTTP 错误无 `.stderr`、无 `.code` → `return false` → 一次失败直接退出
3. **Python 版重试时复用同一 request 对象**：urllib Request 的 data 流可能被消费后无法再读

### 修复内容
- **JS stepAnalyze** (process_videos.js L768-810): 正确解包 retryCall 返回 → `ret.result` 取 fnResult → `fnResult.text` 取实际文本；空内容设置明确错误信息
- **JS isRetryable** (process_videos.js L569-586): 新增 HTTP 5xx 错误匹配 `err.message?.match(/^HTTP\s+5\d{2}/)` → 可重试
- **Python step_analyze** (process_videos.py L774-806): 每次重试重建 urllib Request 对象；status != 200 时捕获响应体；安全访问 `body.get("choices")`
- 两版语法检查均通过
