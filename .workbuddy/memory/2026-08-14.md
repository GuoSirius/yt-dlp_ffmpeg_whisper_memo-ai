# 2026-08-14

## 修复：VIDEO_SHEETS 留空未执行所有 sheet（js/py 双版本）
- 根因：`sheets = targetSheet ? targetSheet : VIDEO_SHEETS`，留空时 `sheets=[]` → 循环 0 次 → 无任务。约定是「留空取全部 sheet」。
- JS：新增 `getAllSheetNames(file)`（XLSX.SheetNames），留空兜底遍历全部；文件循环内解析 sheets（优先级 --sheet > VIDEO_SHEETS > 全部）。
- PY：新增 `get_all_sheet_names(excel_file)`（pd.ExcelFile.sheet_names）兜底；并修复 `run()` 调用笔误 `args.sheets` → `args.sheet`（原 typo 导致 Python 版直接 AttributeError 崩溃，无法启动）。
- 验证（data/examples/website_split.xlsx 含 4 sheet / 108 行）：留空→108 任务；指定 2 sheet→91；--sheet 单 sheet→19。双版本一致。
- 已 commit + push 至 Gitee main（cc7db0f）。README 约定本就正确，无需改。
- commitlint 坑：subject 行首全大写 token（如 VIDEO_SHEETS）判 sentence/start-case → 把大写 token 移到句中才过。
