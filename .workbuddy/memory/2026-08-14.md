# 2026-08-14

## Code Review — video-pipeline 双端流水线
对 `process_videos.py`(4196) / `process_videos.js`(3878) / `console_ui.*` 做了静态 code review，产出 `output/CODE_REVIEW_2026-08-14.md`。

关键发现（按严重度）：
- **B1 并发进度串扰**：`_ffmpeg_state` 是模块级全局（console_ui.py:148 / console-ui.mjs:114），`concurrency>1` 多转码任务共享 → 进度百分比失真；应改为每任务独立 state。
- **B2 终端错乱**：`update_line`/`clear_line`/`Spinner` 无锁（console_ui.py:19-28, 68-112），并发时单行刷新互相覆盖；用 `_print_lock` 包裹或改多行进度。
- **B3 Python 模型竞态**：`_FW_MODEL`/`_FUNASR_MODEL` 全局缓存无锁（process_videos.py:1582-1602, 1755-1784），并发转录会重复加载/竞态；JS 用子进程方式无此问题。
- **M1 死代码**：FunASR 三函数被逐字节复制一遍（process_videos.py:1736-2058，diff 已确认相同），删 1898-2058 即可（git blame 36c6b734）。
- **M2 性能瓶颈**：`write_excel_cell`/`writeExcelCellByKey` 每条任务全量 reload+save 整个 Excel，且末尾 `write_all_contents_to_excel` 再写一遍（双写）；建议关实时写或批量缓存写。
- **M3 指标失真**：download/transcode/transcribe 成功路径 `retries_used` 硬编码 0（process_videos.py:1238,1291,1669…），analyze 正确返回 attempt-1，双端不一致。
- **M4 时钟混用**：`step_analyze` `ai_start=time.time()` 但 `elapsed=time.monotonic()-ai_start`（process_videos.py:983 vs 1083）；JS 用 Date.now() 正确。
- 亮点：断点续跑重校验产物、错误透传到位、无 bare except、JS 已规避 replace `$` 陷阱。

后续优化优先级：P0 修 B1/B2/B3（开并发前必做），P1 删 M1 + 优化 M2，P2 修 M3/M4 + 拆模块。

## 已实施修复（commit 8f993c7，已推 github+gitee main）
B1/B2/B3/M1/M2 全部落地：
- **B1**：`parse_ffmpeg_progress` 增 `state` 参数；`make_ffmpeg_parser`(双端) 每任务建独立 state 字典；JS 增 `makeFfmpegProgressParser` 工厂。删 `reset_ffmpeg_state()` 调用。
- **B2**：`console_ui.py` 增 `_term_lock`(Lock)，`update_line`/`clear_line`/`Spinner.stop` 写 stderr 全走锁。JS 单线程无需锁（写本身原子），仅做 B1。
- **B3**：增 `_model_load_lock`；faster-whisper 与 FunASR 的 `_get_model` 改双检锁。顺带补了原先缺失的模块级 `_FUNASR_MODEL=None`/`_FUNASR_MODEL_CFG=""`（否则 funasr 首次调用会 NameError —— 隐藏 bug）。
- **M1**：用脚本删第二份 FunASR 函数块（1736-2058 → 现 1748-1915 唯一），净减 ~144 行。
- **M2**：Excel 实时写改为「内存缓存 + 周期落盘」。Python：`_EXCEL_WB` 缓存 + `_excel_set_cell` 内存改 + `flush_excel()`（后台线程每 2s / atexit / SIGINT·SIGTERM 兜底）；`write_all_contents_to_excel` 末尾 flush。JS：`_excelWb` 缓存 + 直接写单元格 + `flushExcel()`（setInterval 2s / process exit / SIGINT·SIGTERM）。中断最多丢 2s 修改，远优于丢全部，且不再每条任务串行全量磁盘写。
- 验证：py_compile + node --check 通过；import 冒烟受沙箱缺依赖（colorama/anaconda segfault）所限未跑，逻辑已静态核对。
