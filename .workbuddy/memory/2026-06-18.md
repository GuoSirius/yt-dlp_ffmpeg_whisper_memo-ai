# 2026-06-18 工作日志

## 修复 faster-whisper 和 funasr CLI 实际识别参数错误

### 问题1：JS whisper-ctranslate2 CLI 参数不匹配
- `--num_workers` 不存在于 CLI（是 Python API 参数）→ 移除
- `--vad_onset` 不存在 → CLI 用 `--vad_threshold`（Python API 用 `vad_parameters.onset`）
- 位置：JS `transcribeFasterWhisper` line 1530

### 问题2：JS funasr CLI Hydra 不支持非 ASCII 路径
- Hydra override 解析器只接受 ASCII，中文路径（`普诺赛中文站`）触发 `LexerNoViableAltException`
- 修复：路径含非 ASCII 时自动复制到临时 ASCII 路径（`os.tmpdir()`），执行后清理
- 位置：JS `transcribeFunasrCli` line 1613

### 问题3：JS funasr CLI 结果解析方式错误
- funasr CLI `print(res)` 输出到 stdout，不写文件
- 原代码找 `{input}.json` 文件必然失败
- 修复：优先从 stdout 解析（JSON → Python dict repr 正则提取 `text` 字段 → 纯文本三级回退）
- 位置：JS `transcribeFunasrCli` line 1656

### 验证结果
- funasr CLI 模式：`✔ [16] 识别完成 (2m59s, 659 字符)` ✅ 成功写入 Excel
- faster-whisper 模式：参数不再报 usage 错误（因 HuggingFace 网络问题未能完全验证，但参数修复已确认）

## 全面统一 ASR 后端预检为轻量检测（local / faster-whisper / funasr 三后端统一）

### 问题：whisper-ctranslate2 和 whisper 的 --help 也超时
- 实测耗时：`whisper-ctranslate2 --help` 11.5s / `whisper --help` 4.9s / `import faster_whisper` 9.7s / `import funasr` 20s
- JS `timeout: 5000` 直接 SIGTERM → 误报不可用

### 修复（JS + Python 统一策略）
- **JS** `checkWhisperAvailable`：local + faster-whisper 合并为 `where`/`which` 文件存在检测（3s timeout），funasr 已在上轮修复
- **Python** `_check_whisper_available`：
  - `local` → `shutil.which("whisper")`（CLI）
  - `faster-whisper` → `importlib.util.find_spec("faster_whisper")`（Python 包，0.002s）
  - `funasr/cli` → `importlib.util.find_spec("funasr")`（Python 包，0.000s）
  - `funasr/service` + `service` → HTTP GET（不变）
- 新增 `import importlib.util` 到 Python 顶部

### 验证结果
- 全量检测 < 0.3 秒（JS + Python 均验证通过）
- dry-run 输出 `✅ whisper (faster-whisper (whisper-ctranslate2))` ✅

## 修复 AI 分析 prompt `$` 替换 bug + 添加 AI_DEBUG 日志

### 问题：命令行跑的 AI 分析结果（关键词）与千问桌面不一致
- **根因（JS）**：`promptTpl.replace('{content}', text)` 中 `text` 含 `$&` `$\`` `$'` `$$` 时，
  JS `String.replace` 会特殊解释这些模式，导致发给 AI 的转录文本被损坏。
  例如 `$&` 被替换为匹配的 `{content}`，`$\`` 被替换为匹配前的文本。
  Python `str.replace` 无此问题。
- **修复（JS）**：改用函数替换 `promptTpl.replace('{content}', () => text)`，规避 `$` 特殊语义。
- **新增 AI_DEBUG**：`.env` 设 `AI_DEBUG=true` 时，打印实际发送的 prompt 前 500 字符 + AI 返回内容前 500 字符，
  方便排查"关键词质量不对"类问题。JS + Python 均已添加。
- 位置：JS `stepAnalyze` line 1051；Python `step_analyze` line 1037

## 全面修复 funasr 两种子模式的输出信息（cli / service）

### 涉及文件：process_videos.js + process_videos.py
### 修改点汇总（所有"backend 描述"字符串统一四分支 + funasr 再细分两子模式）：

**JS（process_videos.js）：**
1. `printDryRun` → 上一轮已修复四分支；本轮 funasr 进一步细化：
   - `funasr/cli (paraformer-zh)` 或 `funasr/service (http://localhost:8899)`
2. `checkEnvironmentAsync` issues 字符串同步细化
3. `checkWhisperAvailable` funasr/service 不可用时补充 logError（原来静默返回 false）

**Python（process_videos.py）：**
1. `_check_whisper_available` funasr/service 不可用时补充 log.error；cli 不可用错误信息补充 modelscope
2. `check_environment` issues 字符串从二分支（local/else）→ 四分支，funasr 再细化 cli/service
3. `step_transcribe` 失败时 backend_info 同步四分支 + funasr 子模式细化
4. `_check_and_confirm_env` dry-run 输出的 backend_info 细化 funasr 子模式显示

**最终输出示例：**
- `✅ whisper (funasr/cli (paraformer-zh))`
- `❌ whisper (funasr/service (http://localhost:8899))`

## 修复 funasr 检测 + dry-run 输出信息不匹配问题

### 问题1：dry-run 环境检测显示 `whisper (service http://localhost:9588)` 而非 `funasr/cli`
- **根因**：`printDryRun` 函数（line 3024）的 `backend` 变量只判断了 `local` 和 `service` 两种，
  `faster-whisper` 和 `funasr` 均走 else 分支，被错误显示为 `service ${WHISPER_SERVICE}`。
- **修复**：与 `checkEnvironmentAsync` 保持一致，补充四分支判断：
  `local` / `faster-whisper (whisper-ctranslate2)` / `funasr/${FUNASR_MODE}` / `service ${WHISPER_SERVICE}`

### 问题2：funasr CLI 检测报错"不可用"，实际命令行可用
- **根因1**：原来用 `funasr --help` 做检测，但 funasr 启动时会触发 Hydra 框架初始化，实测耗时 ~20 秒，
  超过 execSync timeout: 5000ms → SIGTERM → 被误判为不可用。
- **根因2（尝试方案）**：换用 `python -c "import funasr"` 同样耗时 ~21 秒（funasr 导入时大量初始化），仍不适用。
- **最终修复**：改用 `where funasr`（Windows）/ `which funasr`（Linux/macOS）检查可执行文件是否存在，
  纯磁盘查找，< 1 秒，准确可靠。
- **注**：`FUNASR_PYTHON` 环境变量保留（原来为 python import 方式引入），现在实际已不使用，可后续清理。
## 2026-06-18 深夜 / 2026-06-19 凌晨：.env / .env.example 格式重整

- 用户反馈 .env 和 .env.example 乱七八糟
- 问题根因：注释与 key=value 位置混乱，部分注释出现在行内（dotenv 会把行内 # 后的内容读进 value）
- 解决：从头重写两个文件，统一规则：
  - 注释独占一行，放在 key=value 上方
  - 不留行内注释（`KEY=value  # comment` 这种格式）
  - 区段标题（╔╗╚╝ 方框）独占一行
  - .env.example 用 `your-xxx-here` 模板值；.env 保留用户真实配置
- 遗留临时脚本 fix_env_comments.py / fix_env_layout.py 已删除
- 提交：chore: 重整 .env 和 .env.example 格式
