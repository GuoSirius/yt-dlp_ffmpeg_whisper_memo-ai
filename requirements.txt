# Python 依赖包列表
# 安装：pip install -r requirements.txt

# 核心依赖
pandas>=1.3.0
openpyxl>=3.0.0
requests>=2.25.0
python-dotenv>=0.19.0
colorama>=0.4.4
questionary>=1.10.0

# ASR 后端（按需安装，至少选一个）
# pip install faster-whisper    # faster-whisper 后端（推荐）
# pip install funasr            # FunASR 后端
# openai-whisper 不需要安装，使用 local 后端时直接调用 whisper CLI

# 可选：开发依赖
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
